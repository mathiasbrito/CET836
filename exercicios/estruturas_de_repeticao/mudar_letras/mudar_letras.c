/* Dada uma string o seu programa deverá trocar todas as letras 
 * (somente as letras e 'z' se torna 'a') pela letra subsequente e após
 * mudar as vogais de minúscula para maiúscula. Exemplo: seja a string
 * "Hello World!" a entrada da sua solução, ela deverá 
 * gerar "Ifmmp XpsmE!". Para testar seu algoritmo, certifique-se que 
 * todas as entradas em entradas.in geram o resultado em saidas.out.
 */

#include <stdio.h>

int main() {
    /* escreva a solução aqui */
    return 0;
}
