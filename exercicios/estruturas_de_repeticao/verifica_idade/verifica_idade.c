/*Crie um programa que leia (scanf) a idade de uma pessoa e o ano de
 * nascimento, imprima (printf) 1 se os valores batem, 0 se não. Exemplo uma
 * pessoa que nasce em 1990 e diz que tem 24 anos, está mentindo portanto o
 * resultado deve ser 0.*/
#include <stdio.h>

int main() {
    /* Seu código aqui */
    return 0;
}
