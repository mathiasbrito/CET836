/* A soma de vetores se dá pela soma de elementos de mesmo índice dos
 * dos vetores, armazenando-se o resultado em celula de mesmo índice
 * no vetor resultado. Sendo assim se X = {1, 2, 3, 4} e Y + {9, 8, 7, 6}
 * o vetor R resultante será R = {10, 10, 10, 10}. Escreva um programa
 * que some dois vetores.
 */

int vetor1[] = {1, 2, 3, 4, 5};
int vetor2[] = {8, 9, 6, 7, 4};

#include <stdio.h>

int main() {
    /* Solução Aqui */
    return 0;
}
