/* Escreva um programa que leia do usuário dois valores em duas varíaveis
 * A e B, inverta os valores de A e B e certifique-se que ao final do 
 * programa os valores foram invertidos imprimindo o resultado na tela*/
